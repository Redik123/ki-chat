[package]
name = "ki-protocol"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
serde.workspace = true
serde_json.workspace = true
# Les vignettes (logos, photos de profil) voyagent en base64 : le contrôle
# de leur en-tête PNG se fait ici, pour que client et serveur appliquent
# exactement la même règle.
base64 = "0.22"
